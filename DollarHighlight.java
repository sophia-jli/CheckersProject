import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class DollarHighlight here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class DollarHighlight extends Highlight
{
    /**
     * Act - do whatever the DollarHighlight wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public DollarHighlight() {
        GreenfootImage image = getImage();
        image.scale(45 , 45);
        setImage(image);
    }
    
    public void act()
    {
        // Add your action code here.
    }
}
